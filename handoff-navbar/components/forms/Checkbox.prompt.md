**Checkbox** — a single boolean option in a form or list. For mutually-exclusive choices use `Radio`.

```jsx
<Checkbox defaultChecked onChange={v => setAgree(v)}>Acepto los términos</Checkbox>
```

Controlled or uncontrolled. The box fills with the accent and shows a rough-stroked tick when on.
