**Spinner / ProgressBar** — loading indicators. Use `Spinner` for indeterminate waits, `ProgressBar` for known completion.

```jsx
<Spinner />
<ProgressBar value={68} />
```

Both use the rough/torn filters for the inked look. Needs the Línea SVG filters.
