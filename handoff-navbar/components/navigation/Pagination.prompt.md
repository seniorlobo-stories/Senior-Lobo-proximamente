**Pagination** — page through long result sets with raised torn-paper tabs.

```jsx
<Pagination pages={[3, 4, 5, '…', 8]} current={4} onChange={setPage} />
```

Insert `'…'` for gaps. Needs the Línea SVG filters.
