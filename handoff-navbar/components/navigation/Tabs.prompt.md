**Tabs** — switch between sibling views. Heavy slab labels (Mick Caster) over a torn underline.

```jsx
<Tabs tabs={['Resumen', 'Detalles', 'Historial']} onChange={setView} />
```

Pass strings (indexed) or `{value,label}` objects. Controlled or uncontrolled.
